{
  "$schema": "https://openclaw.dev/schema/config.json",
  "name": "{{PROJECT_NAME}}",
  "description": "Config mínima para que OpenClaw habite el proyecto. El proyecto se autogobierna con AGENTS.md; este archivo es solo metadata.",
  "agent": {
    "contextFiles": ["AGENTS.md"],
    "ignorePatterns": [
      "node_modules/**",
      ".next/**",
      "dist/**",
      "coverage/**",
      "playwright-report/**",
      "test-results/**"
    ]
  },
  "prompts": {
    "directory": ".openclaw/prompts"
  }
}
